# Stability contract

Version 0.1.x keeps the `anyo.avatar` component name, renderer-neutral root, adapter lifecycle, snapshot version, standard humanoid bone names, expression API, look-at modes, attachment transforms, and locomotion bridge compatible.
